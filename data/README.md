Place the 4-5 assignment PDFs in this directory before running the server.

This repository intentionally ignores PDF files in `data/` so the code can stay public while you control exactly which source documents get indexed.

Keep only the intended corpus PDFs here. Any extra PDF in this folder will also be indexed.
